{
  "name": "power-explained",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev":     "astro dev",
    "build":   "astro build && npx pagefind --site dist --output-path dist/pagefind && node scripts/generate-sitemap.js && node scripts/generate-llms-txt.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.2.1",
    "@shoelace-style/shoelace": "^2.20.1"
  }
}
