# Tag Ontology — power-explained
#
# The authoritative namespace for tags on gestures (and, in time, on
# plateaus, manifests, and other addressable objects).
#
# Validation contract (enforced by scripts/lint-tags.mjs):
#   - Every tag has the form `<namespace>/<term>`.
#   - Unknown NAMESPACES fail the build. Namespaces are deliberate;
#     a new dimension of attention is an architectural decision and
#     should be declared here first.
#   - Unknown TERMS within a known namespace WARN but pass. Terms are
#     organic — places, people, concepts, years accumulate as the book
#     grows. The lint output is the to-add list; we promote terms to
#     this file in batches, not one at a time.
#
# Evolution policy:
#   - Add new namespaces with deliberation. Each is a query axis the
#     book will be sliced along forever.
#   - Add terms freely in code, harvest into the ontology when convenient.
#   - Never remove a term once promoted — gestures may reference it.
#     Mark deprecated with `deprecated: true` and migration notes.
#   - Renaming a term requires a coordinated edit across all gesture
#     files (same discipline as renaming a gesture ID).

version: 1
last_revised: 2026-05-25

namespaces:

  # ===========================================================================
  # plateau — the home plateau of this gesture, mirrored from the `plateau:`
  # field so tag-only queries can still ask "what's in P-04". Lint enforces
  # that the tag matches the field.
  # ===========================================================================
  plateau:
    description: |
      The gesture's home plateau. One per gesture. Must match the
      structured `plateau:` field on the same file.
    cardinality: exactly-one
    terms:
      - P-01
      - P-02
      - P-03
      - P-04
      - P-05
      - P-06
      - P-07
      - P-08
      - P-09
      - P-10
      - P-11
      - P-12
      - P-13
      - P-14
      - P-15
      - P-16
      - P-16b
      - P-17
      - P-18
      - P-OA       # Orgone Accumulator (initiation chapter)
      - P-RE       # Re-Enchanting the World (initiation chapter)
      - P-EP       # Epilogue
      - P-PR       # Prologue
      - P-GC       # The Goetia of Capital (initiation chapter)
      - P-CE       # Chiron / The Centaur Chapter
      - P-IB       # Interlude: Barcelona
      - P-IW       # Interlude: Counter-Hegemony Without the Word (Washington)
      - P-AB       # The Angel Faces Backward (initiation chapter)
      - P-WW       # The Wizard's Walk (initiation chapter — final, pre-Epilogue)

  # ===========================================================================
  # voice — who is speaking in the gesture. A gesture can carry multiple
  # voices if it really does shift mid-flow. A quoted human counts as a
  # voice when the quote is the gesture (Rukia at the memorial).
  # ===========================================================================
  voice:
    description: |
      Who is speaking in the gesture. Quoted humans count when the quote
      IS the gesture. Multiple voices allowed when the gesture genuinely
      braids them.
    cardinality: one-or-more
    terms:
      # The register-keepers
      - jason             # the narrator's primary voice
      - coyote            # truth-in-plain-sight, dismissal-as-seal
      - nasruddin         # oblique fool sharper than the room
      - kropotkin         # fool with field notes; evidence-grounded
      - castaneda         # the apprentice form; seeing as practice
      # Quoted humans (additive; only when their quote is the gesture)
      - arizmendiarrieta
      - rukia-lumumba
      - chokwe-antar-lumumba
      - subcomandante-moises

  # ===========================================================================
  # kind — what kind of rhetorical move the gesture makes. Multiple kinds
  # allowed; a gesture can be genuinely both (e.g. scene + turn).
  # ===========================================================================
  kind:
    description: |
      The rhetorical form of the gesture. Multiple kinds permitted when
      a move is honestly both (a scene that is also a turn; an argument
      that is also a landing). Untagged means "not yet classified" —
      first-pass mechanical splits leave this empty rather than lie.
    cardinality: zero-or-more
    terms:
      - scene             # continuous narrated event
      - argument          # structural claim
      - landing           # single-sentence gesture that lands
      - litany            # rhetorical cascade
      - turn              # confessional or perspectival shift
      - naming            # names what something is
      - recovery          # recovers something dormant
      - aside             # interjection, often voice-shifted
      - prose             # catch-all for unmarked moves (use sparingly)
      - practice          # how-to-do gesture: instructions for a reader practice
      - praise            # blessing / acknowledgment register (P-WW opening)
      - coda              # closing gesture that resolves and exits
      - synthesis         # weaves prior threads into one shape (P-01 closing summaries)
      - orientation       # tells the reader where they are / where the book is going
      - polemic           # commits a contestable claim; takes a side
      - naturalist        # field-observer register: notices, names, lets be

  # ===========================================================================
  # capacity — which of the six reader capacities the gesture builds.
  # ===========================================================================
  capacity:
    description: |
      Reader capacities the gesture cultivates. From the book's
      architecture: NAME, READ, BUILD, HOLD, ENTER, CONTINUE. Untagged
      means "not yet classified" — first-pass mechanical splits leave
      this empty rather than guess.
    cardinality: zero-or-more
    terms:
      - name
      - read
      - build
      - hold
      - enter
      - continue

  # ===========================================================================
  # status — workflow state. Mirrors beauty_check.passed but enables
  # tag-only queries (e.g. "everything not yet passed").
  # ===========================================================================
  status:
    description: |
      Workflow state of the gesture. Mirrors `beauty_check.passed`
      where applicable.
    cardinality: exactly-one
    terms:
      - draft
      - beauty-pending
      - passed

  # ===========================================================================
  # place — where the gesture's events occur or what territory it covers.
  # Open; new places get added as the book grows.
  # ===========================================================================
  place:
    description: |
      The geographic place the gesture is set in or about. Use the most
      specific name that fits. Multiple allowed when a gesture spans
      places.
    cardinality: zero-or-more
    open: true
    terms:
      - mondragon
      - arrasate           # Basque name for Mondragon — kept as alias-aware
      - basque-country
      - spain
      - burlington-vt
      - champlain          # Champlain Housing Trust service area
      - jackson-ms
      - mississippi
      - kerala
      - india
      - crown-heights      # Brooklyn neighborhood
      - brooklyn
      - new-york
      - chiapas
      - mexico
      - oventic
      - norfolk-va
      - virginia
      - valencia
      - ilocos             # Philippines (zanjera)
      - philippines
      - switzerland        # alpine commons
      - asturias           # andecha
      - munich             # Klee 1920, Benjamin purchase 1921
      - paris              # the arcades, the Passagen-Werk
      - portbou            # Benjamin's death on the Spanish border
      - pyrenees           # Benjamin's crossing
      - washington-state   # the Scar (P-AB)
      - hampton-roads      # Hampton Boulevard, Norfolk-Portsmouth-Newport News region
      - zuccotti-park      # Occupy 2011
      - standing-rock      # 2016 prayer camp
      - germany            # Reich's exile origin (P-OA)
      - norway             # Reich's middle exile (P-OA)
      - seattle            # the floor practice setting (P-OA)
      - the-south          # the-shrug's regional locus (P-OA)
      - atlantic-city      # the casino-enclosure case (P-02, P-11, P-14)
      - europe             # the continent — enclosure's origin
      - africa             # the continent — the slave trade, colonization
      - barcelona          # the fish market, the cooperatives (P-IB)
      - france             # P-03, P-07, P-16
      - algeria            # Fanon's Algeria (P-07, P-CE)
      - chelsea-norfolk    # the Norfolk neighborhood (distinct from London Chelsea)
      - narmada            # the Narmada valley, India — Roy's dams
      - turin              # Gramsci's Turin — the factory councils
      - palmares           # Quilombo dos Palmares, Brazil — the maroon polity
      - london
      - berlin
      - italy              # Gramsci's Italy
      - aragon             # the Spanish anarchist collectives (P-03, P-10)

  # ===========================================================================
  # person — historical or named individuals the gesture is about.
  # ===========================================================================
  person:
    description: |
      Named persons the gesture is about. Distinct from `voice` which
      tags whose voice is sounding; a person can appear without speaking.
    cardinality: zero-or-more
    open: true
    terms:
      - arizmendiarrieta
      - sanders-bernie
      - lumumba-chokwe       # the father
      - lumumba-chokwe-antar # the son
      - lumumba-rukia        # the daughter
      - franco               # the dictator (passing reference)
      - reagan               # passing reference
      - pope-francis         # 2015 declaration
      - subcomandante-moises
      - ostrom-elinor
      - hardt                # Michael Hardt — Empire, Commonwealth
      - negri                # Antonio Negri — Empire, Commonwealth
      - gramsci              # Antonio Gramsci — Prison Notebooks, hegemony
      - freire               # Paulo Freire — Pedagogy of the Oppressed
      - luxemburg            # Rosa Luxemburg — The Accumulation of Capital
      - federici             # Silvia Federici — Caliban and the Witch
      - fanon                # Frantz Fanon — The Wretched of the Earth
      - du-bois              # W.E.B. Du Bois — wages of whiteness (P-OA)
      - foucault             # Michel Foucault — biopower, discipline, governmentality
      - deleuze              # Gilles Deleuze — A Thousand Plateaus, Geology of Morals
      - guattari             # Félix Guattari — A Thousand Plateaus, the refrain
      - benjamin             # Walter Benjamin — Theses on the Philosophy of History, the Arcades Project, aura
      - klee                 # Paul Klee — painter of Angelus Novus
      - debord               # Guy Debord — Society of the Spectacle, détournement
      - alexander            # Christopher Alexander — A Pattern Language, the magic of the city
      - prechtel             # Martín Prechtel — the descent requires a community that knows how to receive
      - arendt               # Hannah Arendt — carried Benjamin's Theses out of France
      - plotkin              # Bill Plotkin — Soulcraft, the soul's gift
      - reich                # Wilhelm Reich — character armor, the orgone work (P-OA)
      - freud                # Sigmund Freud — Reich's analyst, the death-drive
      - jung                 # Carl Jung — Reich's contemporary, individuation
      - buddha               # the Buddha — citta-vritti, the floor practice
      - graeber              # David Graeber — Debt, bullshit jobs, the dawn of everything
      - roy-arundhati        # Arundhati Roy — the Narmada dams, the adivasi displaced
      - sorenson             # Richard Sorenson — preconquest consciousness
      - robinson-cedric      # Cedric Robinson — Black Marxism, racial capitalism
      - mbembe               # Achille Mbembe — necropolitics
      - turing               # Alan Turing — computation, the universal machine
      - wynter               # Sylvia Wynter — the overrepresentation of Man
      - baudrillard          # Jean Baudrillard — simulation, the precession of simulacra
      - bookchin             # Murray Bookchin — social ecology, libertarian municipalism
      - marx                 # Karl Marx — primitive accumulation, the commodity form
      - lenin                # Vladimir Lenin — the party, the state
      - stalin               # Joseph Stalin — the terror, the betrayal of the revolution
      - berners-lee          # Tim Berners-Lee — the open web
      - erin                 # recurring companion in Jason's field practice (P-AB, P-OA, P-RE)

  # ===========================================================================
  # concept — book-level concepts and named practices.
  # ===========================================================================
  concept:
    description: |
      Named concepts and practices the gesture invokes. The book's
      structural argument runs through this namespace.
    cardinality: zero-or-more
    open: true
    terms:
      - auzolan
      - andecha
      - bayanihan
      - minga
      - etxe
      - lehen-auzoa
      - commons
      - mutual-aid
      - cooperative
      - clt                  # community land trust
      - federation
      - mandar-obedeciendo
      - caracol
      - junta-de-buen-gobierno
      - prefigurative
      - primitive-accumulation
      - enclosure
      - the-outside          # Luxemburg sense
      - racial-capitalism
      - production-of-subjectivity  # Hardt & Negri — capital produces subjects
      - hegemony             # Gramsci — production of consent, not domination by force
      - conscientization     # Freire — naming the structures that made you silent (ASCII form)
      - biopower             # Foucault — power that manages life, not just threatens death
      - biopolitics          # the contested field between biopower and the subject
      - character-armor      # Reich — somatic hold the body learns under conditions (P-OA, P-WW)
      - aura                 # Benjamin — the unique phenomenon of a distance, however close it may be
      - dialectical-image    # Benjamin — the past flashing up at the instant it can be recognized
      - detournement         # Situationist — turning-back-against, reuse of dominant culture's materials
      - the-collector        # Benjamin — the practice of finding fragments that read against the official story
      - the-wizard           # the figure who reads the wreckage and finds the seed
      - the-council          # the perceptual commons — where what you saw stays seen because others see it
      - the-shard            # the specific fragment held long enough to see what it has always been saying
      - the-line             # the frontline of defense (Zuccotti, Standing Rock, headwaters)
      - the-back-line        # the distributed work of defense — wiring, household, teaching, writing
      - sacer                # Latin — set apart, removed from ordinary use, defended by taboo
      - sacred               # the generalized form of sacer/sanctus/hieros
      - degenerate-art       # Nazi designation; the painter as madman
      - progress             # the storm — the official story of forward motion that buries what it enclosed
      - solidarity           # connection's political name — the act the demons have never been able to administer
      # Power-and-governance vocabulary (theory chapters)
      - civil-society        # Gramsci — the dense web of associations through which hegemony lives and is contested
      - necropolitics        # Mbembe — power expressed as the management of who is allowed to die
      - simulation           # Baudrillard — the map preceding the territory; plans based on appearances
      - capture              # the structural form by which institutions absorb the people they recruit
      # Wizard-book / initiation vocabulary (P-OA, P-WW, etc.)
      - orgone               # Reich's hypothetical bioenergetic field (P-OA)
      - vegetotherapy        # Reich's body-oriented therapy (P-OA)
      - dearmoring           # the practice of unbinding character-armor (P-OA)
      - workers-democracy    # Reich's political form before the box (P-OA)
      - death-drive          # Freud's Thanatos — Reich's break with Freud (P-OA)
      - burnout              # the structural form of armor's collapse (P-OA)
      - discipline           # the practice that holds, not the punishment (P-OA)
      - practice             # the noun: a thing done repeatedly until it knows you (P-OA)
      - mastery              # what practice is and isn't aiming at (P-OA)
      - attention            # the substrate the enclosure tries to enclose (P-OA, P-RE)
      - resonance            # somatic register: bodies responding to bodies (P-OA)
      - being-held           # what community gives that armor was substituting for (P-OA)
      - community            # the formal counter-form to character-armor (P-OA)
      - kokoro               # Japanese — heart-mind, the unified seat (P-OA)
      - spirit               # the generalized form held in P-OA's register
      - citta-vritti         # Sanskrit — the fluctuations of consciousness (P-OA)
      - descent              # the underworld passage that becomes curriculum (P-OA, P-AB)
      - the-shrug            # the gesture as system — Chapter 6's title (P-06)
      - wages-of-whiteness   # Du Bois — the psychic compensation that purchased silence (P-OA)
      - debt                 # Graeber — credit as social relation, enclosed into a product
      - fascism              # the structural form, treated across P-09 and P-OA

  # ===========================================================================
  # year — the time the gesture takes place in or anchors to. Used by
  # the wizard's terrain-reading queries (what does the temporal slice
  # of 1941, 1981, 2020 look like across the book?).
  # ===========================================================================
  year:
    description: |
      The primary year the gesture anchors to. Multiple permitted when
      a gesture spans years (a long-construction gesture). Format YYYY.
    cardinality: zero-or-more
    open: true
    terms:
      - "1941"
      - "1956"
      - "1976"
      - "1981"
      - "1983"
      - "1984"
      - "1991"
      - "1994"
      - "1996"
      - "2003"
      - "2013"
      - "2014"
      - "2015"
      - "2017"
      - "2020"
      - "2023"
      - "2024"
      - "2025"
      - "2026"
      - "1920"
      - "1921"
      - "1933"
      - "1936"
      - "1940"
      - "2011"
      - "2016"
      - "2022"

  # ===========================================================================
  # zone — the gesture's home zone on the territory map. Mirrors the
  # plateau's `map.zone` for tag-only spatial queries.
  # ===========================================================================
  zone:
    description: |
      The territory-map zone the gesture lives in. Inherited from
      the home plateau by default; override per gesture only when a
      gesture genuinely belongs to a different zone (rare).
    cardinality: zero-or-one
    terms:
      - outer-edge
      - clearings
      - high-ridges
      - old-growth
      - second-growth      # regrown forest; the wizard-book practice ground (P-OA)
      - the-river
      - the-wizards-cottage
      - standing-stones

  # ===========================================================================
  # direction — wheel-of-eight direction the gesture sits in. Inherited
  # from plateau by default; override when a gesture genuinely moves the
  # wheel.
  # ===========================================================================
  direction:
    description: |
      The wheel-of-eight direction the gesture sits in. Plateaus carry
      a seat and an axis; gestures can refine — a gesture inside an
      S-seated plateau might genuinely sit at NE (opening) or NW (grief
      processing). Use sparingly; over-tagging here is noise.
    cardinality: zero-or-one
    terms:
      - n
      - ne
      - e
      - se
      - s
      - sw
      - w
      - nw

  # ===========================================================================
  # technology — material technologies the gesture references as part of
  # its system-naming. Distinct from `concept` (an idea) and `place` (a
  # location): a technology is a built artifact whose form has political
  # consequences (the car, the road, the platform, the algorithm).
  # ===========================================================================
  technology:
    description: |
      Material technologies the gesture invokes as agents in the system
      it describes. Use when the technology is doing political work in
      the prose, not just appearing as scenery.
    cardinality: zero-or-more
    open: true
    terms:
      - car                # the automobile as urban-form determinant (P-01)

  # ===========================================================================
  # emotion — the emotional weather of the gesture. Distinct from voice
  # (who is speaking) and concept (what is being argued); emotion is the
  # affective register the prose carries. The book has an emotional spine
  # as much as an argumentative one; this is the axis to query it on.
  # ===========================================================================
  emotion:
    description: |
      The emotional register the gesture carries or evokes. Use sparingly;
      most gestures are emotionally complex. Tag only when one register
      is doing load-bearing work.
    cardinality: zero-or-more
    open: true
    terms:
      - rage               # the rage-at-functioning-system register (P-01)
      - fatigue            # exhaustion, capacity-depletion (P-01, P-OA)
      - disgust            # the moral-disgust pivot that precedes exodus (P-01)
      # --- Tier-2 affective register (analysis/tier2-montecarlo) ---
      - indictment             # naming the crime plainly — 'not hypocrisy, policy'
      - reverence              # devotional attention to the living and the enduring
      - disciplined-hope       # hope that has done the work; not optimism
      - defiance               # the refusal; the line drawn and held
      - grief                  # mourning a specific, known, lost thing
      - wonder                 # awe and curious delight at the world
      - humility               # smallness before what one did not make
      - vindication            # the prediction borne out; the long argument proven
      - conviction             # settled, quiet certainty
      - clear-eyed             # cold analytic clarity; seeing the mechanism without flinch
      - tenderness             # care toward the vulnerable; the soft register
      - unease                 # dread; the sense something is wrong before it is named

  # ===========================================================================
  # action — what happens IN the gesture as an act, distinct from the
  # gesture's rhetorical `kind`. `kind/naming` is a gesture that names;
  # `action/naming` is a gesture in which an act of naming occurs (a
  # character names something). Both can be true; they answer different
  # questions about the same gesture.
  # ===========================================================================
  action:
    description: |
      The act performed within the gesture (someone travels, names,
      exits, votes). Distinct from `kind` (the gesture's rhetorical form).
      A gesture can carry both; they answer different questions.
    cardinality: zero-or-more
    open: true
    terms:
      - travel             # going-to-other-places, often as comparison-source (P-01)
      - naming             # an act of naming-out-loud occurs in the gesture (P-01)
      - narrative          # a narrative is told within the gesture
      - parliamentary      # a procedural / vote / motion act (P-01)
      - exodus             # an act of leaving — quit, resign, walk out (P-01)

  # ===========================================================================
  # image — recurring concrete images the gesture evokes (Tier-2; see
  # analysis/tier2-montecarlo). A figurative-content axis distinct from
  # technology (a built artifact doing political work) and place (a real
  # location): an image is a picture the prose paints.
  # ===========================================================================
  image:
    description: |
      Recurring concrete images the gesture evokes — the book's visual
      vocabulary. Surfaced and validated by multi-pass blind tagging.
    cardinality: zero-or-more
    open: true
    terms:
      - night-herons           # the colony returning each spring to Norfolk
      - the-deer               # the deer that walks into camp; met by stillness
      - the-muskrat            # the muskrat that shows itself; the living world revealing
      - the-doves              # doves on the ground beside the quieted body
      - songbirds              # small wild birds as the living world reading you back
      - old-growth-forest      # the uncut forest; the child standing in it
      - the-redcedar           # the felled western redcedar; the chip/shard kept on a shel
      - mycorrhizal-network    # the underground web of care beneath the visible
      - scattered-seeds        # seeds carried forward; what propagates
      - the-uncut-meadow       # the meadow you can grieve (vs the graph you cannot)
      - the-orchard            # keystone abundance (the goetia tree-ecology)
      - the-armor              # armor made of participation / character armor
      - the-shrug              # the learned bodily posture of diminished expectation
      - the-held-jaw           # the clenched jaw; tension held in the body
      - the-floor              # getting up off the floor; the floor that holds
      - the-mask               # the frozen face/mask worn under power
      - the-mirror             # colonial projection; the self-portrait posing as a mirror
      - the-highway            # the six-lane crossing built for the two-ton body; severanc
      - the-superblock         # asphalt lifted into a planted plaza; depaving
      - common-land-fenced     # the enclosure act made visible — common land fenced off
      - the-7-eleven           # the racial boundary running behind the 7-Eleven
      - the-sidewalk-fridge    # the community fridge on the sidewalk
      - the-restaurant         # Jason's restaurant; service as theatre; after-service wine
      - the-communal-kitchen   # the cooperative/communal food space (and its absence)
      - water-tribunal         # the thousand-year Valencia water court — commons that endu
      - the-bamboo-dam         # Ilocos zanjera bamboo-and-stone dams; commons engineering
      - the-credit-union       # the financial cooperative on the corner
      - the-cooperative-bank   # Mondragón's Caja Laboral; the worker-owned bank
      - the-land-trust         # the community land trust; the ground lease
      - the-occupied-factory   # the Turin factory councils; workers running the machinery
      - zuccotti-park          # Occupy; the square reclaimed
      - the-caracoles          # the Zapatista autonomous councils
      - the-village-assembly   # the village governing itself in public
      - the-circle             # the gathering circle — Freire's literacy circle, the fire 
      - the-coffee-shop        # the regulars; the Tuesday couch
      - the-library            # the public library as commons
      - the-hedgerow           # the hedgerow at dusk; reciprocity with place
      - the-tenant-association # tenants organizing the building
      - the-machine            # the extraction machine that finds new outsides
      - the-operating-system   # the invisible OS running underneath the visible
      - the-interface          # the board / the form as interface to power
      - the-screen             # the phone, the scroll, the app — simulation standing in fo
      - the-rising-river       # the Narmada reservoir rising; Roy waist-deep
      - the-typhoon            # the storm that tests the commons
      - the-thinning-snowpack  # climate loss made local
      - the-thread             # the unbroken thread of builders passed through many hands
      - the-suitcase           # Benjamin's lost suitcase; the manuscript that didn't survi
      - the-foundation-stone   # the stone laid for what others will finish
      - the-prison-cell        # Gramsci's cell; the notebooks written under confinement
      - the-firing-squad       # the execution; the revolution's cost
      - the-plantation         # plantation governance — domination's template
      - the-grimoire           # the book of spells; the book offered as grimoire not curri
      - the-centaur            # Chiron; the herd of centaurs occupying Wall Street
      - the-wizard             # the one who can see and name
      - the-named-demon        # the goetia; naming the demon to bind it
      - the-angel              # Benjamin's angel of history, wings unable to close

  # ===========================================================================
  # motif — recurring figurative through-lines the gesture participates in
  # (return after suppression, knowing in the body). Distinct from concept
  # (an idea argued) and image (a picture): a motif is a repeated move.
  # ===========================================================================
  motif:
    description: |
      Recurring figurative / narrative patterns the gesture participates in.
      The argument carried as repeated moves, not single images.
    cardinality: zero-or-more
    open: true
    terms:
      - return-migration       # return after suppression — the herons; the dormant coopera
      - defense-of-land        # the body placed between the land and its destruction
      - demolished-from-outside # the neighborhood erased by a decision made elsewhere
      - knowing-in-the-body    # knowledge held in practice and the body, not the abstracti
      - body-without-organs    # the body slipping its assigned role (Stonewall/trans/Neo)
      - distillation           # principles distilled from hundreds of real cases (Ostrom)
      - proof-before-theory    # operational, not theoretical — the thing works before it i
      - making-common-sense    # war of position — building the new common sense patiently
      - self-governance-in-public # the assembly governing itself, in the open
      - dissolving-boundary    # class becoming party; structure dissolved to keep building
      - the-lineage            # the inheritance passed hand to hand across generations
      - falling-through-the-gap # the person who falls through the gap between categories
      - trickle-vs-flood       # wealth that should flood but only trickles; flowing the wr
      - cannot-verify-from-inside # you cannot know from inside what the system does to you
      - flowering              # emergence; the unexpected blossoming of the commons

  # ===========================================================================
  # practice — a consciousness practice the book teaches the READER to do
  # (attending, naming, becoming-still). Distinct from action/ (an act a
  # character performs in the gesture) and kind/ (the gesture's rhetorical
  # form): practice is instructional — the verb the reader takes up.
  # ===========================================================================
  practice:
    description: |
      A consciousness practice the book invites the reader to take up —
      the verbs of how to become a wizard. The instructional axis.
    cardinality: zero-or-more
    open: true
    terms:
      - becoming-still         # stilling the body; the pause; quieting enough to perceive
      - naming                 # calling the thing by its true name; the goetia; the spell 
      - greeting               # acknowledging the other — the deer, the heron, the neighbo
      - welcoming              # hospitality; making room; the open door; the sidewalk frid
      - grieving               # allowing the mourning; grief done on purpose, as practice
      - attending              # sustained attention; reading the world; noticing what is a
      - holding                # holding space; bearing the seeing for others; the floor th
      - descending             # going down to soul/underworld and returning bearing a gift
      - de-armoring            # softening; unclenching the jaw; getting up off the floor; 
      - blessing               # benediction; the herons blessed every spring; consecrating
      - witnessing             # bearing witness; letting oneself be read by the living wor
      - tending                # cultivating, watering, keeping; the patient care a commons
      - gathering              # convening; forming the circle; calling people into the sam
      - refusing               # the no; drawing the line and standing on it
      - remembering            # keeping the lineage; passing the thread; refusing to let i
      - practicing             # having a practice at all — the meta-discipline: 'the impor
      - self-governing         # running your own work collectively; the council/cooperativ
      - building               # keep building the alternative in whatever form is availabl
      - continuing             # staying in it; the commons verified by continuing, not by 
      - positioning            # declaring where you stand so others can calibrate the clai
      - conditioning           # arranging the conditions so a connection can happen indire
      - guesting               # entering another's space as guest, not director
      - reciprocating          # returning the gift; the boomerang bottle; watching each ot
