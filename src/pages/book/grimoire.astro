---
import Base from '../../layouts/Base.astro'
import fs from 'node:fs'
import { resolve } from 'path'
import yaml from 'js-yaml'
import { composePlateauBody } from '../../lib/composePlateauBody.mjs'
import { md } from '../../lib/markdown.mjs'

// Staged draft interlude — the grimoire-as-system "legend" (composition pass,
// A1). NOT in the-river.manifest.yml; reachable by direct URL and from the
// threads index for review. Intended home: an interlude adjacent to Part VI.

const MANIFEST = resolve('src/content/book/the-grimoire.md')
const text = fs.readFileSync(MANIFEST, 'utf8')
const fm = yaml.load(text.slice(4, text.indexOf('\n---\n', 4))) || {}
const body = composePlateauBody(MANIFEST)
---
<Base title="The Grimoire — Power Explained" description="How to read a casting. The legend for the threads.">
  <div class="article-wrap">
    <p class="book-nav">
      <a href="/book">← The Book</a>
      <span class="book-nav-sep">·</span>
      <span>Staged draft · the legend</span>
    </p>

    <header class="grim-head">
      <p class="grim-label">{fm.label || 'Interlude'} · {fm.kicker}</p>
      <h1>{fm.h1 || fm.title}</h1>
      {fm.standfirst && <p class="grim-standfirst">{fm.standfirst}</p>}
    </header>

    <div class="body-text grim-body" data-pagefind-body set:html={md(body)} />

    <p class="grim-foot">
      Then go walk a thread. <a href="/book/path">The threads →</a>
    </p>
  </div>
</Base>

<style>
  .book-nav {
    font-family: var(--mono); font-size: 0.65rem; letter-spacing: 0.1em;
    text-transform: uppercase; opacity: 0.4; margin-bottom: 2rem;
    display: flex; gap: 0.5rem; align-items: center;
  }
  .book-nav a { color: inherit; text-decoration: none; }
  .book-nav a:hover { color: var(--red); }
  .book-nav-sep { opacity: 0.4; }

  .grim-head { margin: 0 0 2.5rem; }
  .grim-label {
    font-family: var(--mono); font-size: 0.65rem; letter-spacing: 0.12em;
    text-transform: uppercase; opacity: 0.45; margin-bottom: 0.75rem;
  }
  .grim-standfirst {
    font-size: 0.8rem; line-height: 1.7; opacity: 0.55; max-width: 34rem;
    margin-top: 1rem; font-family: var(--mono);
  }
  .grim-body :global(hr.section-break) {
    border: none; border-top: 1px solid var(--rule); margin: 3rem 0;
  }
  .grim-foot {
    margin: 4rem 0 2rem; font-family: var(--mono); font-size: 0.7rem; opacity: 0.5;
  }
  .grim-foot a { color: var(--red); text-decoration: none; }
  .grim-foot a:hover { text-decoration: underline; }
</style>
